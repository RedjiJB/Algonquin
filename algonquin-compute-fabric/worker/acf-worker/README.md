# acf-worker

The initial worker registers an authorized machine and reports hardware,
operating-system, network, health, and idle-state metadata. It does not execute
remote jobs in the first milestone.
