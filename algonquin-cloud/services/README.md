# Shared Services

- `api-gateway/` — authenticated ingress and routing
- `identity-broker/` — normalized human and service identities
- `service-catalog/` — service registration, discovery, and ownership metadata
- `policy/` — shared policy evaluation primitives
- `events/` — cross-system event publication and subscription
- `spatial-index/` — shared place/scene identity lookup, not product rendering
