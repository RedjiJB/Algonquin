# Applications

User-facing clients of the Algonquin AI Gateway. Clients must not connect to
model providers directly.

- `webui/` — thin downstream OpenWebUI integration
- `code/` — thin downstream OpenCode integration
- `admin/` — operational UI when the gateway needs one
